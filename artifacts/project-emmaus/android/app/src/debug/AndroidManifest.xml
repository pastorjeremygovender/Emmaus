<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:icon="@drawable/ic_geofence_test_launcher"
        android:label="Emmaus Geofence Test"
        android:roundIcon="@drawable/ic_geofence_test_launcher"
        tools:replace="android:icon,android:label,android:roundIcon"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            tools:node="remove" />

        <activity
            android:name=".GeofenceProofActivity"
            android:exported="true"
            android:label="Emmaus Geofence Test"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".GeofenceProofReceiver"
            android:directBootAware="false"
            android:exported="false">
            <intent-filter>
                <action android:name="za.co.emmaus.app.GEOFENCE_PROOF_EVENT" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".GeofenceProofJobService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="true" />
    </application>
</manifest>