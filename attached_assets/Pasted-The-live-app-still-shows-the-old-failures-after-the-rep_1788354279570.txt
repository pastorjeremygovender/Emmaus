The live app still shows the old failures after the reported deployment:

- Content Studio stops at Daily Rhythm Day 6, matching Pastor Jeremy’s personal user progress.
- Pastoral Briefing still does not load.
- Church register, People on Emmaus and Active Walks still display incorrect zero values.
- The same behaviour appears after testing outside the installed PWA.

Therefore, do not treat the previous deployment status as proof that checkpoint 91efeca is serving production.

Do not make additional code changes. Do not run migrations, seeds, backfills, imports, exports or production data syncs. Do not change DATABASE_URL, Secrets, schemas, records or user information.

TASK

1. Identify the exact source revision currently served by:
   
   - https://emmaus.co.za
   - https://www.emmaus.co.za

2. Compare the served application bundles and API behaviour with checkpoint 91efeca.

3. Confirm whether both hostnames route to the same Replit deployment and backend.

4. Inspect deployment and runtime logs for:
   
   - stale deployment revision
   - failed rollout
   - incorrect build directory
   - old frontend bundle
   - old API server
   - custom-domain routing mismatch
   - service-worker or cache versioning problems
   - startup failure or request timeout

5. Confirm that the deployment artifact actually contains the checkpoint 91efeca changes in:
   
   - artifacts/api-server/src/routes/journeys.ts
   - artifacts/api-server/src/lib/daily-rhythm-content-access.ts
   - artifacts/api-server/src/lib/dashboard-store.ts
   - artifacts/api-server/src/lib/pastoral-briefing-store.ts
   - artifacts/project-emmaus/src/pages/admin/pastoral/PastoralHome.tsx

If the active deployment is not serving checkpoint 91efeca, perform one clean deployment of exactly checkpoint 91efeca. Do not include any later or uncommitted changes.

Do not alter database configuration or production data.

After deployment, verify from the live production host—not only from the workspace or preview:

- The served revision is checkpoint 91efeca.
- Both www and non-www hostnames reach the same deployment.
- API health returns 200.
- An administrator receives the complete authored Daily Rhythm catalogue, including days beyond personal Day 6.
- An ordinary user remains limited by normal Daily Rhythm progression.
- Pastoral Briefing succeeds.
- Church register, People on Emmaus and Active Walks return genuine production values.
- No failed request is converted into 0, None or an empty array.
- No database records, schema, Secrets or settings were changed.

If the deployment fails, either hostname routes elsewhere, the live revision cannot be proven, or any verification fails, stop immediately. Report the exact failure and do not retry, modify code or redeploy again.